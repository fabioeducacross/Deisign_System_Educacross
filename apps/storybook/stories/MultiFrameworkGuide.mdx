import { Meta } from "@storybook/blocks";

<Meta title="Getting Started/Multi-Framework Guide" />

# Guia Multi-Framework

Este Design System pode ser usado em **3 frameworks diferentes**. Abaixo estão os exemplos de código para cada componente.

---

## 🔵 Button

### React
```tsx
import { Button } from "@fabioeducacross/ui";

<Button variant="default">Click me</Button>
<Button variant="destructive">Delete</Button>
<Button variant="secondary">Cancel</Button>
<Button size="sm" loading>Loading...</Button>
```

### Vue 2 + Bootstrap
```html
<template>
  <div>
    <button class="btn btn-primary">Click me</button>
    <button class="btn btn-danger">Delete</button>
    <button class="btn btn-outline-secondary">Cancel</button>
    <button class="btn btn-sm" disabled>
      <span class="spinner-border spinner-border-sm"></span>
      Loading...
    </button>
  </div>
</template>
```

### Vue 3
```vue
<template>
  <div>
    <EdButton variant="primary">Click me</EdButton>
    <EdButton variant="danger">Delete</EdButton>
    <EdButton variant="secondary">Cancel</EdButton>
    <EdButton size="sm" :loading="true">Loading...</EdButton>
  </div>
</template>

<script setup lang="ts">
import { EdButton } from '@educacross/ui-vue3';
</script>
```

---

## 📝 Input

### React
```tsx
import { Input } from "@fabioeducacross/ui";

<Input type="email" placeholder="seu@email.com" />
<Input type="password" placeholder="Senha" />
<Input disabled placeholder="Desabilitado" />
```

### Vue 2 + Bootstrap
```html
<template>
  <div>
    <input type="email" class="form-control" placeholder="seu@email.com" />
    <input type="password" class="form-control" placeholder="Senha" />
    <input type="text" class="form-control" disabled placeholder="Desabilitado" />
  </div>
</template>
```

### Vue 3
```vue
<template>
  <div>
    <EdInput type="email" placeholder="seu@email.com" />
    <EdInput type="password" placeholder="Senha" />
    <EdInput disabled placeholder="Desabilitado" />
  </div>
</template>

<script setup lang="ts">
import { EdInput } from '@educacross/ui-vue3';
</script>
```

---

## ☑️ Checkbox

### React
```tsx
import { Checkbox } from "@fabioeducacross/ui";

<div className="flex items-center space-x-2">
  <Checkbox id="terms" />
  <label htmlFor="terms">Aceito os termos</label>
</div>
```

### Vue 2 + Bootstrap
```html
<template>
  <div class="form-check">
    <input type="checkbox" class="form-check-input" id="terms" v-model="accepted" />
    <label class="form-check-label" for="terms">Aceito os termos</label>
  </div>
</template>

<script>
export default {
  data() {
    return {
      accepted: false
    }
  }
}
</script>
```

### Vue 3
```vue
<template>
  <div class="flex items-center space-x-2">
    <EdCheckbox id="terms" v-model="accepted" />
    <label for="terms">Aceito os termos</label>
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue';
import { EdCheckbox } from '@educacross/ui-vue3';

const accepted = ref(false);
</script>
```

---

## 📋 Select

### React
```tsx
import { Select, SelectTrigger, SelectValue, SelectContent, SelectItem } from "@fabioeducacross/ui";

<Select>
  <SelectTrigger>
    <SelectValue placeholder="Selecione" />
  </SelectTrigger>
  <SelectContent>
    <SelectItem value="1">Opção 1</SelectItem>
    <SelectItem value="2">Opção 2</SelectItem>
  </SelectContent>
</Select>
```

### Vue 2 + Bootstrap
```html
<template>
  <select class="form-select" v-model="selected">
    <option value="">Selecione</option>
    <option value="1">Opção 1</option>
    <option value="2">Opção 2</option>
  </select>
</template>

<script>
export default {
  data() {
    return {
      selected: ''
    }
  }
}
</script>
```

### Vue 3
```vue
<template>
  <EdSelect v-model="selected" placeholder="Selecione">
    <EdSelectItem value="1">Opção 1</EdSelectItem>
    <EdSelectItem value="2">Opção 2</EdSelectItem>
  </EdSelect>
</template>

<script setup lang="ts">
import { ref } from 'vue';
import { EdSelect, EdSelectItem } from '@educacross/ui-vue3';

const selected = ref('');
</script>
```

---

## 🎨 Card

### React
```tsx
import { Card, CardHeader, CardTitle, CardContent } from "@fabioeducacross/ui";

<Card>
  <CardHeader>
    <CardTitle>Título do Card</CardTitle>
  </CardHeader>
  <CardContent>
    Conteúdo do card aqui
  </CardContent>
</Card>
```

### Vue 2 + Bootstrap
```html
<template>
  <div class="card">
    <div class="card-header">
      <h5 class="card-title">Título do Card</h5>
    </div>
    <div class="card-body">
      Conteúdo do card aqui
    </div>
  </div>
</template>
```

### Vue 3
```vue
<template>
  <EdCard>
    <EdCardHeader>
      <EdCardTitle>Título do Card</EdCardTitle>
    </EdCardHeader>
    <EdCardBody>
      Conteúdo do card aqui
    </EdCardBody>
  </EdCard>
</template>

<script setup lang="ts">
import { EdCard, EdCardHeader, EdCardTitle, EdCardBody } from '@educacross/ui-vue3';
</script>
```

---

## 🔔 Alert

### React
```tsx
import { Alert, AlertTitle, AlertDescription } from "@fabioeducacross/ui";

<Alert variant="default">
  <AlertTitle>Atenção</AlertTitle>
  <AlertDescription>Mensagem informativa</AlertDescription>
</Alert>
```

### Vue 2 + Bootstrap
```html
<template>
  <div class="alert alert-primary" role="alert">
    <h4 class="alert-heading">Atenção</h4>
    <p>Mensagem informativa</p>
  </div>
</template>
```

### Vue 3
```vue
<template>
  <EdAlert variant="primary">
    <EdAlertTitle>Atenção</EdAlertTitle>
    <EdAlertDescription>Mensagem informativa</EdAlertDescription>
  </EdAlert>
</template>

<script setup lang="ts">
import { EdAlert, EdAlertTitle, EdAlertDescription } from '@educacross/ui-vue3';
</script>
```

---

## 📌 Convenções de Nomenclatura

| React | Vue 2 (Bootstrap) | Vue 3 |
|-------|------------------|-------|
| `variant="default"` | `class="btn-primary"` | `variant="primary"` |
| `variant="destructive"` | `class="btn-danger"` | `variant="danger"` |
| `variant="secondary"` | `class="btn-secondary"` | `variant="secondary"` |
| `disabled` | `:disabled="true"` | `:disabled="true"` |
| `onClick={fn}` | `@click="handleClick"` | `@click="handleClick"` |
| `className` | `class` | `class` |

---

## 🚀 Instalação

### React
```bash
npm install @fabioeducacross/ui
```

### Vue 2
```bash
npm install bootstrap@5
```

### Vue 3
```bash
npm install @educacross/ui-vue3
```
